# Grok 建议 · 2026-09-12 · CR 指针：CR-KHUB-20260911-01

- 条目 ID：GK-20260912-01
- 仓：onewonderjapan/owd-knowledge-hub
- 分支：grok/knowledge
- 模型：grok-4.6 / xhigh（本条为 CR 指针，未再跑长推理）
- 专管 Bot：管知识中枢的
- 类：建议
- 机密分级：L1
- 状态：待开发阅读

## 摘要

已审查并**接受** `claude/review` 上的 `CR-KHUB-20260911-01`（`claude-review/2026-09-11-content-review.md`）。三轨互不合并：CR **保留在** `claude/review`，未合入 `grok/knowledge` / `main`。开发 agent 请直接读该 CR；本条只作指针与选题提醒。

## 依据（可核对引用 / 未核实假设须标明）

- 已核实：`claude/review` tip `632c098` 相对站点底稿仅新增 `claude-review/`（README + 内容 review），未改 `index.html` / `mascot.html` / `design/` / `.github`。
- 已核实：CR 标 L1；P0 指向数据侧 `publish-data.sh` 过滤与 `feature/home-redesign` 页面拦截未合并；明确「不要靠页面正则隐藏内容 / 不要把 kb-data 鉴权化 / 不要内嵌 `#kb-data`」。
- 已核实：与本仓硬边界一致（结构/内容分离、不直写 CDN、建议不进 main）。
- 【未核实假设】`publish-data.sh` 实际过滤逻辑、CDN media Cache-Control、Pages 镜像同步——CR 正文已自标未核实，本条沿用。

## 建议开发 agent 采取的下一步

1. 打开并处理：https://github.com/onewonderjapan/owd-knowledge-hub/blob/claude/review/claude-review/2026-09-11-content-review.md
2. P0 优先走数据侧过滤与（若采纳）拦截段合入 `main` 的正常 PR；不要把 CR 文件 merge 进本轨或 `main`。
3. 采纳/拒绝后回写本条与 CR 条目状态。

## 明确不要做什么

- 不要把 `claude/review` 合进 `grok/knowledge` 或 `grok/feedback`。
- 不要因本指针直接 push `main` 或直写 CDN `kb-data.json`。
- 不要把内部完整版 ops 细节抄进本仓公开路径。
